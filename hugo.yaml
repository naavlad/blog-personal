baseURL: https://blog.weblog-stdout.ru
title: weblog•stdout # site title
theme: PaperMod

outputs:
  home:
    - HTML
    - RSS
    - JSON

enableRobotsTXT: true
buildDrafts: false
buildFuture: false
buildExpired: false

caches:
  images:
    dir: :cacheDir/images

minify:
  disableXML: true
  minifyOutput: true

params:
  env: production # to enable google analytics, opengraph, twitter-cards and schema.
  title: Personal weblog on technology
  description: Заметки о технологиях, которые работают, и тех, что не работают
  keywords: [blog, AI, technology, tools, practical insights]
  author: admin
  images: ["<link or path of image for opengraph, twitter-cards>"]
  DateFormat: "January 2, 2006"
  defaultTheme: auto
  disableThemeToggle: false

  ShowReadingTime: true
  ShowShareButtons: true
  ShowPostNavLinks: true
  ShowBreadCrumbs: true
  ShowCodeCopyButtons: false
  ShowWordCount: true
  ShowRssButtonInSectionTermList: true
  UseHugoToc: true
  disableSpecial1stPost: false
  disableScrollToTop: false
  comments: false
  hidemeta: false
  hideSummary: false
  showtoc: false
  tocopen: false

  assets:
    # disableHLJS: true # to disable highlight.js
    # disableFingerprinting: true
    favicon: "/favicon/favicon.ico"
    favicon16x16: "/favicon/favicon-16x16.png"
    favicon32x32: "/favicon/favicon-32x32.png"
    apple_touch_icon: "/favicon/apple-touch-icon.png"

  label:
    text: "weblog•stdout"
    icon: /apple-touch-icon.png
    iconHeight: 35

  # home-info mode
  homeInfoParams:
    Title: thinking | writing | shipping
    Content: |
      Заметки о технологиях, которые работают, и тех, что не работают  
      📖 Потому что README редко рассказывает всю правду

  # socialIcons:
  #   - name: x
  #     url: "https://x.com/"
  #   - name: stackoverflow
  #     url: "https://stackoverflow.com"
  #   - name: github
  #     url: "https://github.com/"

  cover:
    hidden: false # показывать везде
    hiddenInList: false # показывать в списках (превью)
    hiddenInSingle: true # НЕ показывать в отдельных постах
    responsiveImages: true
    linkFullImages: false

  # for search
  # https://fusejs.io/api/options.html
  fuseOpts:
    isCaseSensitive: false
    shouldSort: true
    location: 0
    distance: 1000
    threshold: 0.4
    minMatchCharLength: 0
    limit: 10 # refer: https://www.fusejs.io/api/methods.html#search
    keys: ["title", "permalink", "summary", "content"]

# Read: https://github.com/adityatelange/hugo-PaperMod/wiki/FAQs#using-hugos-syntax-highlighter-chroma
markup:
  highlight:
    noClasses: false
    # anchorLineNos: true # включает ссылки на номера строк
    codeFences: true # включает подсветку для блоков кода с тройными обратными кавычками ```
    guessSyntax: true # автоматически определяет язык, если не указан
    lineNos: true # включает номера строк
    # style: monokai

menu:
  main:
    - identifier: news
      name: новости
      url: /news/
      title: новости
      weight: 10

    - identifier: projects
      name: проекты
      url: /projects/
      title: проекты
      weight: 15

    - identifier: categories
      name: 📁
      url: /categories/
      title: категории
      weight: 20

    - identifier: tags
      name: 📃
      url: /tags/
      title: теги
      weight: 30

    - identifier: search
      name: 🔍
      url: /search/
      title: поиск
      weight: 40
